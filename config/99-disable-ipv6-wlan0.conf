# WDPi wlan0 IPv6 configuration
#
# The server is intentionally operated as an IPv4-only appliance on its
# trusted local network. Disable IPv6 only on the USB Wi-Fi interface.
#
# This is a sanitized copy of /etc/sysctl.d/99-disable-ipv6-wlan0.conf.

net.ipv6.conf.wlan0.disable_ipv6=1
